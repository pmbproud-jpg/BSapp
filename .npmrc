# Lozniejsza rozdzielczosc peer-dep -- wymagana dla EAS Build po dodaniu
# w fazie 4 zaleznosci testowych (jest, jest-expo, @testing-library/*,
# react-test-renderer). Niektore z nich maja peer ranges nie pasujace
# 1:1 do react 19.1.0 / RN 0.81 / Expo SDK 54, ale dziala dobrze przy
# legacy-peer-deps. Lokalne npm install tez tego uzywa.
legacy-peer-deps=true
